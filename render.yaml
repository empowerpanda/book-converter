# Render 一鍵部署：書籍轉換工具
# 在 Dashboard 選「New > Blueprint」並連到此 repo 即可依此檔建立 Web Service
services:
  - type: web
    name: book-converter
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT app:app
    healthCheckPath: /api/health

[project]
name = "book-converter"
version = "0.1.0"
description = "書籍轉換工具：簡體/英文 epub → 臺灣繁體"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
app = "app:app"

[tool.vercel]
framework = "flask"
